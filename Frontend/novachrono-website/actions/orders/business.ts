// Currently no hooks for orders (as it's mostly POST), but keeping file for consistency
import * as services from './service';

// Example if we need to fetch orders later
// import useSWR from 'swr';
